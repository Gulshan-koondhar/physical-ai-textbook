import React from 'react';
// Import the StarboardNotebook component
import StarboardNotebook from '../components/StarboardNotebook';

// Define the components that will be available globally in MDX files
const MDXComponents = {
  StarboardNotebook,
};

export default MDXComponents;