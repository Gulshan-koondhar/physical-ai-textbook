---
title: Nav2 and MoveIt 2 for Humanoid Robotics AI
sidebar_label: Nav2 and MoveIt 2
description: Implement navigation and manipulation planning using Nav2 and MoveIt 2 for AI-powered humanoid robots
keywords: [Nav2, MoveIt 2, navigation, manipulation, planning, humanoid, AI]
---

# Nav2 and MoveIt 2 for Humanoid Robotics AI

## Introduction

Navigation and manipulation are fundamental capabilities for humanoid robots, requiring sophisticated planning algorithms to operate in complex environments. Nav2 (Navigation 2) provides state-of-the-art navigation capabilities for mobile robots, while MoveIt 2 offers comprehensive motion planning for manipulator arms. For humanoid robots with both locomotion and manipulation capabilities, integrating both systems is essential for autonomous operation.

This chapter covers the configuration, setup, and integration of Nav2 and MoveIt 2 for humanoid robotics applications, focusing on AI-driven planning and execution.

## Understanding Nav2 for Humanoid Locomotion

### Nav2 Architecture Overview

Nav2 is built on ROS 2 and provides a flexible, behavior-based navigation system:

1. **Navigation Server**: Main coordination node
2. **Planners**: Global and local path planners
3. **Controllers**: Trajectory controllers
4. **Recovery Behaviors**: Fallback strategies
5. **Lifecycle Manager**: State management

### Nav2 Components for Humanoid Robots

For humanoid robots, Nav2 requires specific configurations:

- **Costmap Integration**: For uneven terrain navigation
- **Footstep Planning**: For bipedal locomotion
- **Humanoid Controllers**: For walking gaits
- **Dynamic Obstacle Avoidance**: For crowded environments

### Basic Nav2 Configuration

```yaml
# config/nav2_params.yaml
amcl:
  ros__parameters:
    use_sim_time: True
    alpha1: 0.2
    alpha2: 0.2
    alpha3: 0.2
    alpha4: 0.2
    alpha5: 0.2
    base_frame_id: "base_footprint"
    beam_count: 60
    initial_pose.x: 0.0
    initial_pose.y: 0.0
    initial_pose.z: 0.0
    initial_pose.yaw: 0.0
    lambda_short: 0.1
    max_beams: 60
    max_particles: 2000
    min_particles: 500
    pf_err: 0.05
    pf_z: 0.5
    recovery_alpha_fast: 0.0
    recovery_alpha_slow: 0.0
    resample_interval: 1
    robot_model_type: "nav2_amcl::DifferentialMotionModel"
    save_pose_rate: 0.5
    scan_matching_score_threshold: 0.0
    scan_number_of_beams: 60
    scan_probability_max_dist: 0.5
    sigma_hit: 0.2
    tf_broadcast: true
    transform_tolerance: 1.0
    update_min_a: 0.2
    update_min_d: 0.1
    z_hit: 0.5
    z_max: 0.05
    z_rand: 0.5
    z_short: 0.05

amcl_map_client:
  ros__parameters:
    use_sim_time: True

amcl_rclcpp_node:
  ros__parameters:
    use_sim_time: True

bt_navigator:
  ros__parameters:
    use_sim_time: True
    global_frame: map
    robot_base_frame: base_footprint
    odom_topic: /odom
    bt_loop_duration: 10
    default_server_timeout: 20
    enable_groot_monitoring: True
    groot_zmq_publisher_port: 1666
    groot_zmq_server_port: 1667
    # Specify the path to the BT xml file
    # default_nav_through_poses_bt_xml: "navigate_w_replanning_and_recovery.xml"
    # default_nav_to_pose_bt_xml: "navigate_w_replanning_and_recovery.xml"
    plugin_lib_names:
    - nav2_compute_path_to_pose_action_bt_node
    - nav2_compute_path_through_poses_action_bt_node
    - nav2_smooth_path_action_bt_node
    - nav2_follow_path_action_bt_node
    - nav2_spin_action_bt_node
    - nav2_wait_action_bt_node
    - nav2_assisted_teleop_action_bt_node
    - nav2_back_up_action_bt_node
    - nav2_drive_on_heading_bt_node
    - nav2_clear_costmap_service_bt_node
    - nav2_is_stuck_condition_bt_node
    - nav2_goal_reached_condition_bt_node
    - nav2_goal_updated_condition_bt_node
    - nav2_globally_updated_goal_condition_bt_node
    - nav2_is_path_valid_condition_bt_node
    - nav2_initial_pose_received_condition_bt_node
    - nav2_reinitialize_global_localization_service_bt_node
    - nav2_rate_controller_bt_node
    - nav2_distance_controller_bt_node
    - nav2_speed_controller_bt_node
    - nav2_truncate_path_action_bt_node
    - nav2_truncate_path_local_action_bt_node
    - nav2_goal_updater_node_bt_node
    - nav2_recovery_node_bt_node
    - nav2_pipeline_sequence_bt_node
    - nav2_round_robin_node_bt_node
    - nav2_transform_available_condition_bt_node
    - nav2_time_expired_condition_bt_node
    - nav2_path_expiring_timer_condition
    - nav2_distance_traveled_condition_bt_node
    - nav2_single_trigger_bt_node
    - nav2_is_battery_low_condition_bt_node
    - nav2_navigate_through_poses_action_bt_node
    - nav2_navigate_to_pose_action_bt_node
    - nav2_remove_passed_goals_action_bt_node
    - nav2_planner_selector_bt_node
    - nav2_controller_selector_bt_node
    - nav2_goal_checker_selector_bt_node
    - nav2_controller_cancel_bt_node
    - nav2_path_longer_on_approach_bt_node
    - nav2_wait_cancel_bt_node
    - nav2_spin_cancel_bt_node
    - nav2_back_up_cancel_bt_node
    - nav2_assisted_teleop_cancel_bt_node
    - nav2_drive_on_heading_cancel_bt_node

bt_navigator_rclcpp_node:
  ros__parameters:
    use_sim_time: True

controller_server:
  ros__parameters:
    use_sim_time: True
    controller_frequency: 20.0
    min_x_velocity_threshold: 0.001
    min_y_velocity_threshold: 0.5
    min_theta_velocity_threshold: 0.001
    failure_tolerance: 0.3
    progress_checker_plugin: "progress_checker"
    goal_checker_plugin: "goal_checker"
    controller_plugins: ["FollowPath"]

    # Humanoid-specific controller
    FollowPath:
      plugin: "nav2_mppi_controller::MPPIController"
      time_steps: 50
      model_dt: 0.05
      batch_size: 1000
      vx_std: 0.2
      vy_std: 0.1
      wz_std: 0.3
      vx_max: 0.5
      vx_min: -0.2
      vy_max: 0.3
      wz_max: 0.3
      sim_frequency: 20.0
      control_frequency: 20.0
      motion_model: "DiffDrive"
      xy_goal_tolerance: 0.25
      yaw_goal_tolerance: 0.25
      stateful: true
      collision_check: true
      collision_cost: 1000.0
      goal_angle_cost: 2.0
      goal_pos_cost: 1.0
      path_angle_cost: 2.0
      path_pos_cost: 1.0
      twirling_cost: 0.0
      nonholonomic: true
      transform_tolerance: 0.5
      goal_checker: "simple_goal_checker"
      path_dist_checker: "none"
      obstacle_cost_method: "costmap"
      reference_path_checker: "none"
      goal_checker: "simple_goal_checker"

controller_server_rclcpp_node:
  ros__parameters:
    use_sim_time: True

local_costmap:
  local_costmap:
    ros__parameters:
      update_frequency: 5.0
      publish_frequency: 2.0
      global_frame: odom
      robot_base_frame: base_footprint
      use_sim_time: True
      rolling_window: true
      width: 6
      height: 6
      resolution: 0.05
      robot_radius: 0.3
      plugins: ["voxel_layer", "inflation_layer"]
      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        cost_scaling_factor: 3.0
        inflation_radius: 0.55
      voxel_layer:
        plugin: "nav2_costmap_2d::VoxelLayer"
        enabled: True
        publish_voxel_map: False
        origin_z: 0.0
        z_resolution: 0.2
        z_voxels: 10
        max_obstacle_height: 2.0
        mark_threshold: 0
        observation_sources: scan
        scan:
          topic: /scan
          max_obstacle_height: 2.0
          clearing: True
          marking: True
          data_type: "LaserScan"
          raytrace_max_range: 3.0
          raytrace_min_range: 0.0
          obstacle_max_range: 2.5
          obstacle_min_range: 0.0
      always_send_full_costmap: True
  local_costmap_client:
    ros__parameters:
      use_sim_time: True
  local_costmap_rclcpp_node:
    ros__parameters:
      use_sim_time: True

global_costmap:
  global_costmap:
    ros__parameters:
      update_frequency: 1.0
      publish_frequency: 1.0
      global_frame: map
      robot_base_frame: base_footprint
      use_sim_time: True
      robot_radius: 0.3
      resolution: 0.05
      track_unknown_space: false
      plugins: ["static_layer", "obstacle_layer", "inflation_layer"]
      obstacle_layer:
        plugin: "nav2_costmap_2d::ObstacleLayer"
        enabled: True
        observation_sources: scan
        scan:
          topic: /scan
          max_obstacle_height: 2.0
          clearing: True
          marking: True
          data_type: "LaserScan"
          raytrace_max_range: 3.0
          raytrace_min_range: 0.0
          obstacle_max_range: 2.5
          obstacle_min_range: 0.0
      static_layer:
        plugin: "nav2_costmap_2d::StaticLayer"
        map_subscribe_transient_local: True
      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        cost_scaling_factor: 3.0
        inflation_radius: 0.55
      always_send_full_costmap: True
  global_costmap_client:
    ros__parameters:
      use_sim_time: True
  global_costmap_rclcpp_node:
    ros__parameters:
      use_sim_time: True

map_server:
  ros__parameters:
    use_sim_time: True
    yaml_filename: "turtlebot3_world.yaml"

map_saver:
  ros__parameters:
    use_sim_time: True
    save_map_timeout: 5.0
    free_thresh_default: 0.25
    occupied_thresh_default: 0.65

planner_server:
  ros__parameters:
    expected_planner_frequency: 20.0
    use_sim_time: True
    planner_plugins: ["GridBased"]
    GridBased:
      plugin: "nav2_navfn_planner::NavfnPlanner"
      tolerance: 0.5
      use_astar: false
      allow_unknown: true

planner_server_rclcpp_node:
  ros__parameters:
    use_sim_time: True

smoother_server:
  ros__parameters:
    use_sim_time: True
    smoother_plugins: ["simple_smoother"]
    simple_smoother:
      plugin: "nav2_smoother::SimpleSmoother"
      tolerance: 0.01
      max_its: 1000
      do_refinement: True

behavior_server:
  ros__parameters:
    costmap_topic: local_costmap/costmap_raw
    footprint_topic: local_costmap/published_footprint
    cycle_frequency: 10.0
    behavior_plugins: ["spin", "backup", "wait", "assisted_teleop", "drive_on_heading"]
    spin:
      plugin: "nav2_behaviors::Spin"
      spin_dist: 1.57
    backup:
      plugin: "nav2_behaviors::BackUp"
      backup_dist: 0.15
      backup_speed: 0.025
    wait:
      plugin: "nav2_behaviors::Wait"
      wait_duration: 1.0
    assisted_teleop:
      plugin: "nav2_behaviors::AssistedTeleop"
      min_vel_trans: 0.05
      max_vel_trans: 0.5
      min_vel_theta: 0.1
      max_vel_theta: 0.5
      scale_factor: 1.5
    drive_on_heading:
      plugin: "nav2_behaviors::DriveOnHeading"
      min_duration: 0.5
      max_duration: 60.0
      min_heading_dist: 0.5
      max_heading_dev: 1.57
    global_frame: odom
    robot_base_frame: base_footprint
    transform_tolerance: 0.1
    use_sim_time: true

waypoint_follower:
  ros__parameters:
    loop_rate: 20
    stop_on_failure: false
    waypoint_task_executor_plugin: "wait_at_waypoint"
    wait_at_waypoint:
      plugin: "nav2_waypoint_follower::WaitAtWaypoint"
      enabled: true
      waypoint_pause_duration: 200
```

## Humanoid-Specific Nav2 Configuration

### Custom Footstep Planner

For bipedal humanoid navigation, implement a footstep planner:

```python
# scripts/footstep_planner.py
import rclpy
from rclpy.node import Node
from nav2_msgs.action import NavigateToPose
from geometry_msgs.msg import PoseStamped
import numpy as np

class FootstepPlanner(Node):
    def __init__(self):
        super().__init__('footstep_planner')

        # Create action client for navigation
        self.nav_to_pose_client = ActionClient(
            self, NavigateToPose, 'navigate_to_pose')

    def plan_footsteps(self, start_pose, goal_pose):
        """Plan safe footsteps for bipedal locomotion"""
        # Calculate footstep sequence based on humanoid kinematics
        footsteps = []

        # Implement footstep planning algorithm
        # This would consider step length, foot clearance, balance
        step_length = 0.3  # meters
        step_width = 0.2   # meters

        # Calculate path with safe footstep locations
        path = self.calculate_path(start_pose, goal_pose)

        # Generate alternating left/right foot steps
        for i, point in enumerate(path):
            foot_pose = PoseStamped()
            foot_pose.pose.position = point
            # Add foot clearance and orientation
            foot_pose.pose.position.z = 0.02  # Small clearance

            if i % 2 == 0:
                # Left foot step
                foot_pose.pose.position.y += step_width / 2
            else:
                # Right foot step
                foot_pose.pose.position.y -= step_width / 2

            footsteps.append(foot_pose)

        return footsteps

    def calculate_path(self, start, goal):
        """Calculate safe path considering humanoid constraints"""
        # Implement path planning with humanoid-specific constraints
        # Consider step limits, balance, and terrain
        pass

def main(args=None):
    rclpy.init(args=args)
    node = FootstepPlanner()

    try:
        rclpy.spin(node)
    except KeyboardInterrupt:
        pass
    finally:
        node.destroy_node()
        rclpy.shutdown()

if __name__ == '__main__':
    main()
```

### Humanoid Navigation Controller

```yaml
# config/humanoid_nav2_params.yaml
controller_server:
  ros__parameters:
    use_sim_time: True
    controller_frequency: 10.0  # Lower frequency for humanoid walking
    min_x_velocity_threshold: 0.05
    min_y_velocity_threshold: 0.05
    min_theta_velocity_threshold: 0.05
    failure_tolerance: 0.5
    progress_checker_plugin: "progress_checker"
    goal_checker_plugin: "goal_checker"
    controller_plugins: ["HumanoidController"]

    # Humanoid-specific controller
    HumanoidController:
      plugin: "nav2_mppi_controller::MPPIController"
      time_steps: 30
      model_dt: 0.1
      batch_size: 500
      vx_std: 0.1
      vy_std: 0.05
      wz_std: 0.15
      vx_max: 0.3  # Slower for humanoid stability
      vx_min: -0.1
      vy_max: 0.1
      wz_max: 0.2
      sim_frequency: 10.0
      control_frequency: 10.0
      motion_model: "HumanoidWalk"
      xy_goal_tolerance: 0.3  # Larger for humanoid precision
      yaw_goal_tolerance: 0.3
      stateful: true
      collision_check: true
      collision_cost: 1000.0
      goal_angle_cost: 1.5
      goal_pos_cost: 1.0
      path_angle_cost: 1.5
      path_pos_cost: 1.0
      twirling_cost: 0.5  # Penalize unnecessary rotation
      nonholonomic: true
      transform_tolerance: 1.0
      goal_checker: "simple_goal_checker"
      path_dist_checker: "none"
      obstacle_cost_method: "costmap"
      reference_path_checker: "none"
      goal_checker: "simple_goal_checker"
```

## Understanding MoveIt 2 for Humanoid Manipulation

### MoveIt 2 Architecture

MoveIt 2 provides motion planning for manipulator arms:

1. **MoveGroup Interface**: High-level planning interface
2. **Planning Scene**: World representation
3. **Motion Planners**: Path planning algorithms
4. **Trajectory Execution**: Controller interfaces
5. **Collision Detection**: Safety checking

### MoveIt 2 Configuration for Humanoid Arms

```yaml
# config/humanoid_moveit/config/controllers.yaml
controller_manager:
  ros__parameters:
    update_rate: 100  # Hz

    # Joint trajectory controllers
    left_arm_controller:
      type: position_controllers/JointTrajectoryController

    right_arm_controller:
      type: position_controllers/JointTrajectoryController

    left_hand_controller:
      type: position_controllers/JointTrajectoryController

    right_hand_controller:
      type: position_controllers/JointTrajectoryController

# Left arm controller configuration
left_arm_controller:
  ros__parameters:
    joints:
      - left_shoulder_pitch
      - left_shoulder_yaw
      - left_shoulder_roll
      - left_elbow
      - left_wrist_pitch
      - left_wrist_yaw
    interface_name: position

# Right arm controller configuration
right_arm_controller:
  ros__parameters:
    joints:
      - right_shoulder_pitch
      - right_shoulder_yaw
      - right_shoulder_roll
      - right_elbow
      - right_wrist_pitch
      - right_wrist_yaw
    interface_name: position

# Hand controllers
left_hand_controller:
  ros__parameters:
    joints:
      - left_thumb_joint
      - left_index_joint
      - left_middle_joint
    interface_name: position

right_hand_controller:
  ros__parameters:
    joints:
      - right_thumb_joint
      - right_index_joint
      - right_middle_joint
    interface_name: position
```

### MoveIt 2 Setup Assistant Configuration

```yaml
# config/humanoid_moveit/config/humanoid.srdf
<?xml version="1.0" encoding="UTF-8"?>
<!--This does not replace URDF, and is not an extension of URDF.
    This is a format for representing semantic information about the robot structure.
    A URDF file must exist for this robot as well, where the joints and the links that are referenced are defined
-->
<robot name="humanoid">
    <!--GROUPS: Representation of a set of joints and links. This can be useful for specifying DOF to plan for, defining arms, end effectors, etc-->
    <!--LINKS: When a link is specified, the parent joint of that link (if it exists) is automatically included-->
    <!--JOINTS: When a joint is specified, the child link of that joint (which will always exist) is automatically included-->
    <!--CHAINS: When a chain is specified, all the links along the chain (including endpoints) are included in the group. Additionally, all the joints that are parents to included links are also included. This means that joints along the chain and the parent joint of the base link are included in the group-->
    <!--SUBGROUPS: Groups can also be formed by referencing to already defined group names-->
    <group name="left_arm">
        <chain base_link="torso_link" tip_link="left_hand_link"/>
    </group>
    <group name="right_arm">
        <chain base_link="torso_link" tip_link="right_hand_link"/>
    </group>
    <group name="left_hand">
        <joint name="left_thumb_joint"/>
        <joint name="left_index_joint"/>
        <joint name="left_middle_joint"/>
    </group>
    <group name="right_hand">
        <joint name="right_thumb_joint"/>
        <joint name="right_index_joint"/>
        <joint name="right_middle_joint"/>
    </group>
    <group name="both_arms">
        <group name="left_arm"/>
        <group name="right_arm"/>
    </group>
    <group name="upper_body">
        <group name="both_arms"/>
        <joint name="torso_yaw"/>
        <joint name="torso_pitch"/>
    </group>

    <!--GROUP STATES: Purpose: Define a named state for a particular group, in terms of joint values. This is useful to define states like 'folded arms'-->
    <group_state name="left_arm_ready" group="left_arm">
        <joint name="left_shoulder_pitch" value="0"/>
        <joint name="left_shoulder_yaw" value="0"/>
        <joint name="left_shoulder_roll" value="0"/>
        <joint name="left_elbow" value="1.57"/>
        <joint name="left_wrist_pitch" value="0"/>
        <joint name="left_wrist_yaw" value="0"/>
    </group_state>
    <group_state name="right_arm_ready" group="right_arm">
        <joint name="right_shoulder_pitch" value="0"/>
        <joint name="right_shoulder_yaw" value="0"/>
        <joint name="right_shoulder_roll" value="0"/>
        <joint name="right_elbow" value="1.57"/>
        <joint name="right_wrist_pitch" value="0"/>
        <joint name="right_wrist_yaw" value="0"/>
    </group_state>

    <!--END EFFECTOR: Purpose: Represent information about an end effector.-->
    <end_effector name="left_hand" parent_link="left_hand_link" group="left_hand" parent_group="left_arm"/>
    <end_effector name="right_hand" parent_link="right_hand_link" group="right_hand" parent_group="right_arm"/>

    <!--VIRTUAL JOINT: Purpose: this element defines a virtual joint between a robot link and an external frame of reference (considered fixed with respect to the robot)-->
    <virtual_joint name="virtual_joint" type="fixed" parent_frame="world" child_link="base_link"/>

    <!--DISABLE COLLISIONS: By default it is assumed that any link of the robot could potentially come into collision with any other link in the robot. This tag disables collision checking between a specified pair of links. -->
    <disable_collisions link1="base_link" link2="torso_link" reason="Adjacent"/>
    <disable_collisions link1="left_upper_arm_link" link2="left_lower_arm_link" reason="Adjacent"/>
    <disable_collisions link1="right_upper_arm_link" link2="right_lower_arm_link" reason="Adjacent"/>
    <!-- Add more collision pairs as needed -->
</robot>
```

## MoveIt 2 Motion Planning for Humanoid Arms

### Basic Motion Planning Example

```python
# scripts/humanoid_moveit_example.py
import rclpy
from rclpy.node import Node
from moveit_msgs.action import MoveGroup
from geometry_msgs.msg import Pose
from moveit_msgs.msg import RobotState
from sensor_msgs.msg import JointState
from rclpy.action import ActionClient

class HumanoidMoveItNode(Node):
    def __init__(self):
        super().__init__('humanoid_moveit_node')

        # Initialize MoveGroup action client
        self.move_group_client = ActionClient(
            self, MoveGroup, 'move_group')

        # Initialize joint state subscriber
        self.joint_state_sub = self.create_subscription(
            JointState, '/joint_states', self.joint_state_callback, 10)

        self.current_joint_state = JointState()

    def joint_state_callback(self, msg):
        """Update current joint state"""
        self.current_joint_state = msg

    def plan_to_pose(self, group_name, target_pose):
        """Plan motion to target pose"""
        goal_msg = MoveGroup.Goal()

        # Set planning group
        goal_msg.request.group_name = group_name

        # Set target pose
        goal_msg.request.workspace_parameters.header.frame_id = "base_link"

        # Create pose constraint
        pose_constraint = self.create_pose_constraint(
            target_pose, group_name)
        goal_msg.request.goal_constraints.append(pose_constraint)

        # Set planning options
        goal_msg.request.num_planning_attempts = 10
        goal_msg.request.allowed_planning_time = 5.0
        goal_msg.request.max_velocity_scaling_factor = 0.5
        goal_msg.request.max_acceleration_scaling_factor = 0.5

        return goal_msg

    def create_pose_constraint(self, target_pose, link_name):
        """Create pose constraint for motion planning"""
        from moveit_msgs.msg import Constraints
        from moveit_msgs.msg import PositionConstraint
        from moveit_msgs.msg import OrientationConstraint
        from geometry_msgs.msg import Point
        import moveit_msgs.msg

        constraint = Constraints()

        # Position constraint
        pos_constraint = PositionConstraint()
        pos_constraint.link_name = link_name
        pos_constraint.header.frame_id = "base_link"
        pos_constraint.target_point_offset.x = 0.0
        pos_constraint.target_point_offset.y = 0.0
        pos_constraint.target_point_offset.z = 0.0
        pos_constraint.constraint_region.primitives.append(
            moveit_msgs.msg.SolidPrimitive(
                type=moveit_msgs.msg.SolidPrimitive.SPHERE,
                dimensions=[0.01]))  # 1cm tolerance
        pos_constraint.weight = 1.0

        # Orientation constraint
        orient_constraint = OrientationConstraint()
        orient_constraint.link_name = link_name
        orient_constraint.header.frame_id = "base_link"
        orient_constraint.orientation = target_pose.orientation
        orient_constraint.absolute_x_axis_tolerance = 0.1
        orient_constraint.absolute_y_axis_tolerance = 0.1
        orient_constraint.absolute_z_axis_tolerance = 0.1
        orient_constraint.weight = 1.0

        constraint.position_constraints.append(pos_constraint)
        constraint.orientation_constraints.append(orient_constraint)

        return constraint

    def execute_trajectory(self, trajectory):
        """Execute planned trajectory"""
        # Implementation for trajectory execution
        # This would interface with ros2_control
        pass

def main(args=None):
    rclpy.init(args=args)
    node = HumanoidMoveItNode()

    try:
        rclpy.spin(node)
    except KeyboardInterrupt:
        pass
    finally:
        node.destroy_node()
        rclpy.shutdown()

if __name__ == '__main__':
    main()
```

## AI-Enhanced Planning Integration

### Combining Nav2 and MoveIt 2

Create a unified planning system that coordinates navigation and manipulation:

```python
# scripts/unified_planning_node.py
import rclpy
from rclpy.node import Node
from geometry_msgs.msg import PoseStamped, PointStamped
from moveit_msgs.msg import MoveItErrorCodes
from nav2_msgs.action import NavigateToPose
from moveit_msgs.action import MoveGroup
from rclpy.action import ActionClient
import json

class UnifiedPlanningNode(Node):
    def __init__(self):
        super().__init__('unified_planning_node')

        # Action clients
        self.nav_client = ActionClient(self, NavigateToPose, 'navigate_to_pose')
        self.moveit_client = ActionClient(self, MoveGroup, 'move_group')

        # Publishers and subscribers
        self.task_sub = self.create_subscription(
            String, '/planning_tasks', self.task_callback, 10)

        self.status_pub = self.create_publisher(
            String, '/planning_status', 10)

    def task_callback(self, msg):
        """Handle incoming planning tasks"""
        task = json.loads(msg.data)

        if task['type'] == 'navigation':
            self.execute_navigation_task(task)
        elif task['type'] == 'manipulation':
            self.execute_manipulation_task(task)
        elif task['type'] == 'combined':
            self.execute_combined_task(task)

    def execute_navigation_task(self, task):
        """Execute navigation-only task"""
        goal_pose = PoseStamped()
        goal_pose.pose.position.x = task['goal']['x']
        goal_pose.pose.position.y = task['goal']['y']
        goal_pose.pose.position.z = task['goal']['z']
        goal_pose.pose.orientation.w = 1.0

        goal_msg = NavigateToPose.Goal()
        goal_msg.pose = goal_pose

        self.nav_client.wait_for_server()
        future = self.nav_client.send_goal_async(goal_msg)
        future.add_done_callback(self.navigation_done_callback)

    def execute_manipulation_task(self, task):
        """Execute manipulation-only task"""
        goal_msg = MoveGroup.Goal()
        goal_msg.request.group_name = task['group']

        # Set target pose or joint positions
        if 'pose' in task:
            pose_constraint = self.create_pose_constraint(
                task['pose'], task['end_effector'])
            goal_msg.request.goal_constraints.append(pose_constraint)
        elif 'joints' in task:
            joint_constraint = self.create_joint_constraint(
                task['joints'], task['group'])
            goal_msg.request.goal_constraints.append(joint_constraint)

        self.moveit_client.wait_for_server()
        future = self.moveit_client.send_goal_async(goal_msg)
        future.add_done_callback(self.manipulation_done_callback)

    def execute_combined_task(self, task):
        """Execute combined navigation and manipulation task"""
        # First navigate to approach position
        nav_task = {
            'type': 'navigation',
            'goal': task['approach_pose']
        }

        # After navigation completes, execute manipulation
        self.execute_navigation_task(nav_task)
        # The manipulation will be triggered after navigation completes

    def navigation_done_callback(self, future):
        """Handle navigation completion"""
        goal_result = future.result()

        if goal_result.status == GoalStatus.STATUS_SUCCEEDED:
            self.get_logger().info('Navigation completed successfully')
            # If there's a manipulation task after navigation
            # self.execute_manipulation_task(self.pending_manipulation_task)
        else:
            self.get_logger().error('Navigation failed')

    def manipulation_done_callback(self, future):
        """Handle manipulation completion"""
        goal_result = future.result()

        if goal_result.status == GoalStatus.STATUS_SUCCEEDED:
            self.get_logger().info('Manipulation completed successfully')
        else:
            self.get_logger().error('Manipulation failed')

def main(args=None):
    rclpy.init(args=args)
    node = UnifiedPlanningNode()

    try:
        rclpy.spin(node)
    except KeyboardInterrupt:
        pass
    finally:
        node.destroy_node()
        rclpy.shutdown()

if __name__ == '__main__':
    main()
```

## Launch Files for Integrated System

### Complete Navigation and Manipulation Launch

```python
# launch/humanoid_nav_manipulation.launch.py
from launch import LaunchDescription
from launch.actions import DeclareLaunchArgument, IncludeLaunchDescription
from launch.conditions import IfCondition
from launch.launch_description_sources import PythonLaunchDescriptionSource
from launch.substitutions import LaunchConfiguration, PathJoinSubstitution
from launch_ros.actions import Node
from launch_ros.substitutions import FindPackageShare

def generate_launch_description():
    # Launch configuration variables
    use_sim_time = LaunchConfiguration('use_sim_time', default='false')
    params_file = LaunchConfiguration('params_file', default='config/nav2_params.yaml')
    autostart = LaunchConfiguration('autostart', default='true')
    use_composition = LaunchConfiguration('use_composition', default='False')
    use_respawn = LaunchConfiguration('use_respawn', default='False')

    # Paths
    pkg_nav2_bringup = FindPackageShare('nav2_bringup')
    pkg_moveit_ros_benchmarks = FindPackageShare('moveit_ros_benchmarks')

    # Declare launch arguments
    declare_use_sim_time = DeclareLaunchArgument(
        'use_sim_time',
        default_value='false',
        description='Use simulation (Gazebo) clock if true'
    )

    declare_params_file = DeclareLaunchArgument(
        'params_file',
        default_value='config/nav2_params.yaml',
        description='Full path to the ROS2 parameters file to use for all launched nodes'
    )

    declare_autostart = DeclareLaunchArgument(
        'autostart',
        default_value='true',
        description='Automatically startup the nav2 stack'
    )

    declare_use_composition = DeclareLaunchArgument(
        'use_composition',
        default_value='False',
        description='Whether to use composed bringup'
    )

    declare_use_respawn = DeclareLaunchArgument(
        'use_respawn',
        default_value='False',
        description='Whether to respawn if a node crashes'
    )

    # Launch Nav2
    nav2_bringup_cmd = IncludeLaunchDescription(
        PythonLaunchDescriptionSource(
            PathJoinSubstitution([
                FindPackageShare('nav2_bringup'),
                'launch',
                'navigation_launch.py'
            ])
        ),
        launch_arguments={
            'use_sim_time': use_sim_time,
            'params_file': params_file,
            'autostart': autostart,
            'use_composition': use_composition,
            'use_respawn': use_respawn
        }.items()
    )

    # Launch MoveIt 2
    moveit_cmd = IncludeLaunchDescription(
        PythonLaunchDescriptionSource(
            PathJoinSubstitution([
                FindPackageShare('humanoid_moveit_config'),
                'launch',
                'move_group.launch.py'
            ])
        ),
        launch_arguments={
            'use_sim_time': use_sim_time
        }.items()
    )

    # Unified planning node
    unified_planning_node = Node(
        package='humanoid_planning',
        executable='unified_planning_node',
        name='unified_planning_node',
        parameters=[{'use_sim_time': use_sim_time}],
        output='screen'
    )

    # Create launch description
    ld = LaunchDescription()

    # Add launch arguments
    ld.add_action(declare_use_sim_time)
    ld.add_action(declare_params_file)
    ld.add_action(declare_autostart)
    ld.add_action(declare_use_composition)
    ld.add_action(declare_use_respawn)

    # Add launch commands
    ld.add_action(nav2_bringup_cmd)
    ld.add_action(moveit_cmd)
    ld.add_action(unified_planning_node)

    return ld
```

## AI-Driven Task Planning

### Behavior Trees for Complex Tasks

Integrate behavior trees for high-level task planning:

```xml
<!-- config/complex_task_bt.xml -->
<root main_tree_to_execute="MainTree">
    <BehaviorTree ID="MainTree">
        <Sequence>
            <ReactiveSequence>
                <IsTaskAvailable />
                <UpdateTaskQueue />
            </ReactiveSequence>
            <Fallback>
                <Sequence>
                    <IsObjectGraspable />
                    <NavigateToApproachPose />
                    <GraspObject />
                </Sequence>
                <Sequence>
                    <IsNavigationRequired />
                    <NavigateToPose />
                </Sequence>
                <Idle />
            </Fallback>
        </Sequence>
    </BehaviorTree>

    <BehaviorTree ID="GraspObject">
        <Sequence>
            <DetectObject />
            <PlanGraspPose />
            <MoveToGraspPose />
            <CloseGripper />
            <LiftObject />
        </Sequence>
    </BehaviorTree>
</root>
```

## Performance Optimization

### Multi-Threaded Execution

Configure Nav2 and MoveIt 2 for optimal performance:

```yaml
# config/performance_params.yaml
# Nav2 performance settings
controller_server:
  ros__parameters:
    use_sim_time: True
    controller_frequency: 20.0  # Adjust based on humanoid walking speed
    thread_count: 4  # Multi-threaded planning
    enable_stomp: true  # Enable STOMP for smoother paths

planner_server:
  ros__parameters:
    use_sim_time: True
    thread_count: 4
    enable_lazy_prm: true  # Enable faster planning

# MoveIt 2 performance settings
move_group:
  ros__parameters:
    use_sim_time: True
    planning_attempts: 10
    planning_time: 5.0
    max_velocity_scaling_factor: 0.5
    max_acceleration_scaling_factor: 0.5
    collision_detector: "FCL"  # Use FCL for better performance
    trajectory_execution:
      allowed_start_tolerance: 0.01
      execution_duration_monitoring: True
      allowed_execution_duration_scaling: 1.2
      allowed_goal_duration_margin: 0.5
```

## Troubleshooting Common Issues

### Navigation Issues

#### Issue: Robot Gets Stuck in Local Minima
**Solutions**:
- Increase local costmap inflation radius
- Use more sophisticated planners (SMP, STOMP)
- Implement better recovery behaviors
- Adjust controller parameters for smoother motion

#### Issue: Footstep Planning for Uneven Terrain
**Solutions**:
- Use 3D costmaps for elevation awareness
- Implement terrain classification
- Add footstep optimization algorithms
- Consider using specialized humanoid navigation packages

### Manipulation Issues

#### Issue: Inverse Kinematics Failures
**Solutions**:
- Increase joint limits tolerance
- Use redundancy resolution
- Implement multiple IK solvers as fallback
- Add collision-aware IK constraints

#### Issue: Trajectory Execution Failures
**Solutions**:
- Verify ros2_control configuration
- Check joint state publisher
- Validate controller configuration
- Use trajectory monitoring and recovery

## Best Practices for Humanoid Robotics

### 1. Hierarchical Planning

Implement hierarchical planning for complex humanoid tasks:

```python
class HierarchicalPlanner:
    def __init__(self):
        self.task_planner = TaskPlanner()
        self.motion_planner = MotionPlanner()
        self.recovery_planner = RecoveryPlanner()

    def plan_task(self, task):
        """Plan at different levels of abstraction"""
        # High-level task planning
        high_level_plan = self.task_planner.plan(task)

        # Mid-level motion planning
        mid_level_plan = []
        for subtask in high_level_plan:
            motion_plan = self.motion_planner.plan(subtask)
            mid_level_plan.append(motion_plan)

        # Low-level execution
        return self.execute_plan(mid_level_plan)
```

### 2. Safety and Robustness

Implement comprehensive safety checks:

```python
class SafetyManager:
    def __init__(self):
        self.balance_checker = BalanceChecker()
        self.collision_checker = CollisionChecker()
        self.emergency_stopper = EmergencyStopper()

    def check_safety(self, planned_motion):
        """Verify safety before execution"""
        if not self.balance_checker.is_stable(planned_motion):
            return False, "Balance violation"

        if self.collision_checker.will_collide(planned_motion):
            return False, "Collision detected"

        return True, "Safe to execute"
```

### 3. Adaptive Planning

Implement adaptive planning based on environment and robot state:

```python
class AdaptivePlanner:
    def __init__(self):
        self.environment_analyzer = EnvironmentAnalyzer()
        self.robot_state_analyzer = RobotStateAnalyzer()
        self.planner_selector = PlannerSelector()

    def adapt_planning_strategy(self, current_state, environment):
        """Select appropriate planning strategy"""
        env_type = self.environment_analyzer.analyze(environment)
        robot_state = self.robot_state_analyzer.analyze(current_state)

        planner = self.planner_selector.select(env_type, robot_state)
        return planner
```

## Integration with AI Systems

### Perception-Action Integration

Connect perception systems with planning systems:

```python
class PerceptionActionIntegration:
    def __init__(self):
        self.perception_sub = self.create_subscription(
            ObjectDetectionArray, '/object_detections',
            self.detection_callback, 10)

        self.planning_pub = self.create_publisher(
            PlanningRequest, '/planning_requests', 10)

    def detection_callback(self, detections):
        """Process detections and generate planning requests"""
        for detection in detections.detections:
            if detection.label == 'graspable_object':
                self.request_grasp_planning(detection)
            elif detection.label == 'obstacle':
                self.update_navigation_map(detection)
```

## Summary

In this chapter, you've learned:
- How to configure Nav2 for humanoid robot navigation with bipedal locomotion
- How to set up MoveIt 2 for humanoid arm manipulation
- How to integrate navigation and manipulation planning systems
- Configuration files and launch files for both systems
- AI-driven planning approaches for complex humanoid tasks
- Performance optimization techniques
- Troubleshooting strategies for common issues
- Best practices for safe and robust humanoid operation
- Integration techniques with perception and AI systems

Nav2 and MoveIt 2 provide the foundation for autonomous humanoid robot operation, enabling complex navigation and manipulation tasks. Proper integration of both systems is essential for creating capable, intelligent humanoid robots.

## Exercises

1. Configure Nav2 for a humanoid robot with custom parameters
2. Set up MoveIt 2 for a humanoid arm with 6+ DOF
3. Create a unified planning system that coordinates navigation and manipulation
4. Implement a behavior tree for complex humanoid tasks
5. Optimize the planning system for your specific humanoid robot

<StarboardNotebook>
```python
# This would be an interactive code block for students to experiment with Nav2 and MoveIt 2
# In a real implementation, this would connect to ROS 2 services
# to test navigation and manipulation planning
```
</StarboardNotebook>